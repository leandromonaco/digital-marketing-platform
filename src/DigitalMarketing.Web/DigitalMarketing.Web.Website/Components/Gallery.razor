@using DigitalMarketing.Model.Settings
@inject DigitalMarketing.Web.Model.TenantConfigurationSession SiteState
@inject IJSRuntime JSRuntime

<section class="bg-dark">
    <div class="container">
        <div class="row">
            <div class="col-sm-12 text-center">
                <h4 class="uppercase mb16">@Section.Title</h4>
                <p class="lead mb64">
                    @Section.Paragraph
                </p>
            </div>
        </div>

        <div class="row">
            <div class="col-sm-12">
                <div class="lightbox-grid square-thumbs" data-gallery-title="Gallery">
                    <ul>
                        @foreach (var image in @Section.Images)
                        {
                            <li>
                                <div class="background-image-holder">
                                    <img alt="image" class="background-image" src="@image">
                                </div>
                            </li>
                        }
                    </ul>
                </div>

            </div>
        </div>

    </div>

</section>

@code {
    [Parameter]
    public DigitalMarketing.Model.Settings.Section Section { get; set; }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await JSRuntime.InvokeAsync<object>("SetGallery");
    }
}
